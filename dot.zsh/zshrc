# Load plugins
# ---------------------------------------------------**
. "$HOME/.helpers"
. "$HOME/.zplug/init.zsh"
. "$HOME/.tmux/tmuxinator.zsh"

# Configuration files for use in Zsh
# ---------------------------------------------------**
. "$HOME/.zshrc.env"
. "$HOME/.zshrc.config"
. "$HOME/.zshrc.alias"
. "$HOME/.zshrc.function"
. "$HOME/.zshrc.local"
. "$HOME/.zshrc.setting"
. "$HOME/.zshrc.plugin"

# When OSX or Linux
# ---------------------------------------------------**
case "${OSTYPE}" in
  darwin*)
    . "$HOME/.zshrc.osx"
    ;;

  linux*)
    . "$HOME/.zshrc.linux"
    ;;
esac

[user]
  name = Satoshi Ohmori
  email = sachin21dev@gmail.com
[core]
  editor = vim
  excludesfile = ~/.global_ignore
[github]
  user = Satoshi Ohmori
[url "git@github.com:"]
  pushinsteadof = https://github.com
[ghq]
  root = ~/Projects
[alias]
  open = "!vim $(git diff --name-only)"
  sync = !git checkout master && git pull origin master && git fetch -p origin && git branch -d $(git branch --merged | grep -v master | grep -v '*')
  syncd = !git checkout develop && git pull origin master && git fetch -p origin && git branch -d $(git branch --merged | grep -v master | grep -v '*')
  syncm = !git checkout master && git pull upstream master && git fetch -p origin && git branch -d $(git branch --merged | grep -v master | grep -v '*')
[push]
  default = matching
[tig "bind"]
  generic = g move-first-line
  generic = G move-last-line

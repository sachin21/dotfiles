# Using my helpers
# ---------------------------------------------------**
. "$HOME/.helpers"

# When OSX or Linux
# ---------------------------------------------------**
case "${OSTYPE}" in
  darwin*)
    . "$HOME/.zshrc.osx"
    ;;

  linux*)
    . "$HOME/.zshrc.linux"
    ;;
esac

# Load plugins
# ---------------------------------------------------**
. "$HOME/.zplug/init.zsh"
. "$HOME/.tmux/tmuxinator.zsh"

# Configuration files for use in Zsh
# ---------------------------------------------------**
. "$HOME/.zshrc.env"
. "$HOME/.zshrc.config"
. "$HOME/.zshrc.setting"
. "$HOME/.zshrc.alias"
. "$HOME/.zshrc.function"
. "$HOME/.zshrc.plugin"
. "$HOME/.zshrc.local"

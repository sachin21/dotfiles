function agvim () {
  vim $(ag $@ | peco --query "$LBUFFER" | awk -F : '{print "-c " $2 " " $1}')
}

function cr() {
 cd `ghq list -p | peco`
 zle accept-line
 zle clear-screen
}

zle -N cr
bindkey '^s' cr
